═══════════════════════════════════════════════════════════════
  COMO EXPORTAR DADOS DO BACKEND LOCAL PARA O RENDER
═══════════════════════════════════════════════════════════════

📋 PRÉ-REQUISITOS:

1. ✅ Backend local rodando e funcionando
2. ✅ Render backend criado e deployado
3. ✅ Admin criado no Render (via /api/setup)
4. ✅ Imagens devem estar no Render (backend/uploads/)

═══════════════════════════════════════════════════════════════

🚀 PASSO A PASSO:

1️⃣  VERIFICAR BACKEND LOCAL:
   
   Execute: cd backend && python verificar_artigos.py
   
   Deve mostrar:
   - Total de artigos (você tem 8)
   - Total de usuários (você tem 5)

2️⃣  CRIAR ADMIN NO RENDER (se ainda não criou):
   
   Execute no navegador ou via curl:
   
   POST https://futmz.onrender.com/api/setup
   
   Isso cria:
   - Usuário admin
   - Credenciais: admin / admin123
   - Alguns artigos de exemplo (se ainda não tiver)

3️⃣  VERIFICAR IMAGENS:
   
   As imagens dos artigos locais devem estar no Render.
   
   Se as imagens estão em backend/uploads/ localmente:
   - Faça commit e push para GitHub
   - O Render vai receber as imagens
   
   OU
   
   - Upload manual via endpoint /api/upload-image

4️⃣  EXECUTAR EXPORTAÇÃO:
   
   Opção A - Script Batch (Windows):
   - Execute: EXPORTAR_PARA_RENDER.bat
   
   Opção B - Manual:
   - cd backend
   - python exportar_para_render.py

5️⃣  VERIFICAR RESULTADO:
   
   Acesse: https://futmz.onrender.com/api/articles
   
   Ou use o script de teste:
   - cd backend
   - python testar_backend_local.py (mas apontando para Render)

═══════════════════════════════════════════════════════════════

⚠️  IMPORTANTE:

- O script faz login automaticamente com: admin / admin123
- Artigos duplicados serão pulados (não geram erro)
- URLs de imagens são convertidas automaticamente para o Render
- Usuários NÃO são exportados (devem ser criados manualmente)

═══════════════════════════════════════════════════════════════

🔧 TROUBLESHOOTING:

❌ Erro: "Não foi possível conectar ao Render"
   → Verifique se o Render está online
   → Acesse: https://futmz.onrender.com/api/health

❌ Erro: "Login falhou"
   → Execute primeiro: POST /api/setup no Render
   → Ou crie manualmente o admin via /api/users/register

❌ Erro: "Artigo já existe"
   → Normal! O script pula artigos duplicados
   → Não é um erro crítico

❌ Imagens não aparecem
   → Verifique se as imagens estão em backend/uploads/
   → Faça commit e push para GitHub
   → O Render precisa ter as imagens no diretório uploads/

═══════════════════════════════════════════════════════════════

📊 DADOS QUE SERÃO EXPORTADOS:

✅ Artigos (8 no total):
   - Título, conteúdo, categoria
   - URLs de imagens (convertidas para Render)
   - Status (publicado/rascunho)
   - Featured (destaque)
   - Tags, excerpt, etc.

❌ Usuários NÃO são exportados:
   - Devem ser criados manualmente
   - Use /api/users/register ou /api/setup

═══════════════════════════════════════════════════════════════

🎯 DEPOIS DA EXPORTAÇÃO:

1. Verifique os artigos no Render:
   https://futmz.onrender.com/api/articles

2. Teste no app mobile:
   - Os artigos devem aparecer na aba INÍCIO
   - As imagens devem carregar corretamente

3. Se tudo funcionar:
   - ✅ Dados sincronizados entre local e Render!

═══════════════════════════════════════════════════════════════
